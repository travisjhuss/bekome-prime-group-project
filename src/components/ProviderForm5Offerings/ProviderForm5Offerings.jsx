import { useSelector } from 'react-redux';
import { Paper, TextField, Typography, Grid } from '@material-ui/core';

function ProviderForm5Offerings({ classes, handleInputs }) {
  return <Paper className={classes.paper} elevation={4}></Paper>;
}

export default ProviderForm5Offerings;
